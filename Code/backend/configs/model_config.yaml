logistic_regression:
  max_iter: 1000
  class_weight: balanced

decision_tree:
  random_state: 42
  class_weight: balanced

random_forest:
  n_estimators: 300
  random_state: 42
  class_weight: balanced
  n_jobs: -1

svm:
  probability: true
  class_weight: balanced

xgboost:
  n_estimators: 300
  max_depth: 8
  learning_rate: 0.05
  subsample: 0.8
  colsample_bytree: 0.8
  random_state: 42
